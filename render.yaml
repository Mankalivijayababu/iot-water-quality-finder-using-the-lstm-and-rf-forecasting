services:
  - type: web
    name: iot-water-quality-finder
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    runtime: python3
    envVars:
      PYTHON_VERSION: 3.10.4
